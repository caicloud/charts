name: Redis-Cluster
briefDesc: Redis 是一个开源 key-value 数据库
desc: |
  Redis-Cluster 是 redis 的集群模式，redis 是一个使用 ANSI C 编写的开源、支持网络、基于内存、可选持久性的键值对存储数据库。
  它支持以下数据类型：字符串，哈希表，列表，集合，有序集合，位图，hyperloglog，支持半径查询的地理空间索引。
  Redis 内置了复制，Lua 脚本，LRU 回收，事务和不同级别持久化存储等功能，通过 Redis Sentinel 来支持高可用，通过 Redis
  Cluster 来支持自动分区。
  从2015年6月开始，Redis 的开发由 Redis Labs 赞助，而2013年5月至2015年6月期间，其开发由 Pivotal 赞助。
  在2013年5月之前，其开发由 VMware 赞助。根据月度排行网站 DB-Engines.com 的数据显示，Redis 是最流行的键值对存储数据库。
introduce:
  brief: |
    基于 Redis 社区版本（Repo(https://github.com/docker-library/redis) - [Dockerfile](https://github.com/quasimodo7614/redis-cluster)）
  howToUse: |
    直接从模板部署出一个完整的 Redis 应用，不需要任何外部依赖。
    **如何挂载数据**
    数据挂载在 `/data`
    配置挂载在 `/usr/local/etc/redis/redis.conf`
    **配置**
    更加详细的配置说明请看[自解释的配置](https://raw.githubusercontent.com/antirez/redis/4.0/redis.conf)
    **关于扩容**
    支持扩容
  version: |
    基于社区 4.0.6 稳定版本。
  resources: |
    最低资源要求：CPU：1 Core，内存：2 GiB，1个节点
    推荐资源配置：CPU：4 Core，内存：4 GiB，2个以上节点
  service: |
    默认使用集群内部 IP 暴露服务的 6379 端口
    如果要提供对外服务, 可以使用节点端口或者对接负载均衡来暴露服务。
